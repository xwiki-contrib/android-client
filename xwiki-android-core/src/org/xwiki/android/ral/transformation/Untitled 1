consider: apply Builder pattern. 
So, which REST model to use can be identified from the config.xml and convert accordingly.

This is currently achieved by creating a separate ReSTfulManager for each ReST scheme we use. (performance efficient)